# Angular21NFInjector

This Angular application uses Native Federation for microfrontends.

## Launch the App

Run the following command to start both the shell and the microfrontend:

```bash
npm start
```

The shell will be available at http://localhost:4200 and the microfrontend at http://localhost:4201.
